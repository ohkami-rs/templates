mod utils;
mod fetch;
mod components;

use fetch::Client;
use utils::{set_state, report_error};
use components::{};

use crate::models::{};
use yew::prelude::*;


#[function_component]
pub fn App() -> Html {
    html! (
    )
}
